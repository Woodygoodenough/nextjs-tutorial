import DemoLoginButton from "@/components/demo-login-button";
import HeroSectionClient from "@/components/hero-section-client";

export default function HeroSection() {
    return <HeroSectionClient demoCta={<DemoLoginButton />} />;
}
